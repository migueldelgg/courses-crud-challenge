version: '3.8'

services:
  porstgres:
    container_name: curso_crud
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=curso_crud_postgres